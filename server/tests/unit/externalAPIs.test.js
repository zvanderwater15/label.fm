describe("last.fm", () => {
  test("get user", async () => {});
  test("user not found", async () => {});
  test("time out, try for 30 seconds then return an error", async () => {});
});

describe("musicbrainz", () => {
  test("get record labels for album", async () => {});
  test("time out, try for 30 seconds then return an error", async () => {});
});
